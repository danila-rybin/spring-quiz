spring:
  main:
    log-startup-info: true # логирование деталей старта разрешено
    banner-mode: off

logging.level:
  ru.yandex.practicum.quiz: TRACE # настраиваем уровень логера

spring-quiz:
  report:
    mode: "concise"